# Generated corpora and staged copies of customer projects.
work/
projects/

# Raw benchmark output. Findings are summarised in ../BENCHMARKS.md; the JSON
# and logs are large, machine-specific, and regenerated by run_sweep.sh.
results/

# Python bytecode generated while running benchmark helpers.
__pycache__/
*.py[cod]

# Local machine paths (copy of bench.env.example).
bench.env
