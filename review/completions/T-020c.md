# T-020c: Replace hardcoded `al_keywords` array in code_actions.rs
## Status: COMPLETED
## Changes
- File: `crates/al-core/src/queries/code_actions.rs`
- What: Replaced the incomplete local `al_keywords` array with `al_syntax::language_data::is_keyword()`; retained `//` and `end;` as explicit prefix checks since they are not grammar keywords.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
The local array was a subset of actual AL keywords, causing missed keyword recognition in code-action filtering; `is_keyword()` covers the full set maintained by the language-data generator.
