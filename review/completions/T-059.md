# T-059: Add unit tests for `references.rs` and `folding.rs`
## Status: COMPLETED
## Changes
- File: `crates/al-core/src/queries/references.rs`, `crates/al-core/src/queries/folding.rs`
- What: Added positive tests (correct reference/fold ranges returned for known AL code) and negative tests (empty/unknown input returns empty result) for both public query functions.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
Both functions had zero coverage, meaning regressions in either would go undetected until manual Zed testing; the added tests provide a fast, automated safety net for future changes.
