# T-038: Fix zed-al platform detection and DAP binary resolution
## Status: COMPLETED
## Changes
- File: `src/platform.rs`, `src/dap.rs`
- What: Added `USERPROFILE`/`HOMEDRIVE` fallback checks for native Windows; unified the 4-step binary resolution logic between LSP and DAP path; restricted DAP launch-arg defaults to Launch scenarios only.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
Without `USERPROFILE` checks the extension silently failed to find the binary on Windows; duplicated resolution logic between LSP and DAP caused the two transports to behave differently for the same binary-search rules.
