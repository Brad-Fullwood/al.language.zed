# T-051: Fix al-zed-test `#[ignore]` for live tests and `wait_for_regex` naming
## Status: COMPLETED
## Changes
- File: `crates/al-zed-test/tests/live_test.rs`, `crates/al-zed-test/src/lsp_log.rs`
- What: Added `#[ignore]` to all `live_test.rs` tests (require a running Zed instance); replaced early-return guards with `#[ignore]`; renamed `wait_for_regex` to `wait_for_substring` to match actual implementation; fixed `\r\n` line-ending handling inconsistency.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
Tests that silently pass without exercising anything (due to early-return guards) are indistinguishable from real passes in CI; `#[ignore]` makes the requirement explicit and prevents misleading green results.
