# T-011/012: Remove LSP Types from al-core Public API
## Status: COMPLETED (in worktree, needs dedicated merge)
## Worktree: `.claude/worktrees/agent-af66adea` (branch `worktree-agent-af66adea`)
## Changes
- 16 files, +399/-85 lines
- New types in `queries/mod.rs`: `AlSymbolKind`, `AlDocumentSymbol`, `AlFoldingRange`, `AlInlayHint`
- Query functions return transport-agnostic types instead of `tower_lsp::lsp_types`
- `From` conversions in both directions
- `al-lsp/handlers.rs` converts at boundary
## Validation
- `cargo check`: PASS
- `cargo clippy -D warnings`: PASS
- `cargo test`: 583 al-core + al-lsp tests pass
## Merge Notes
Conflicts with main due to overlapping `.into()` fixes applied earlier. Needs manual merge or cherry-pick from the worktree branch. The worktree is self-consistent and passes all CI checks independently.
## Justification
Architecture rule: "All business logic lives in al-core. LSP types should NOT appear in al-core's public query return types." This removes the coupling between al-core and tower_lsp, making al-core usable from non-LSP transports (daemon, CLI).
