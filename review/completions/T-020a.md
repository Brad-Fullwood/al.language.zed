# T-020a: Replace hardcoded `SINGLE_STMT_OPENERS` with LanguageData
## Status: COMPLETED
## Changes
- File: `crates/al-syntax/src/formatting.rs`
- What: Removed the `SINGLE_STMT_OPENERS` const array of `(keyword, delimiter)` tuples and replaced with `LanguageData::single_stmt_openers()` backed by the JSON data file.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
Any new AL control-flow keywords would require a code change under the old approach; the LanguageData-driven lookup keeps formatting correct for future BC releases automatically.
