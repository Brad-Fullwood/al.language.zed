# T-020: Replace hardcoded object type list in xliff.rs
## Status: COMPLETED
## Changes
- File: `crates/al-core/src/xliff.rs`
- What: Replaced the hardcoded `object_types` array literal with a call to `al_syntax::find_object_declaration` / `LanguageData` to enumerate AL object types at runtime.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
The hardcoded list would miss any new BC object types added in future releases; using `LanguageData` ensures XLIFF processing stays correct as the language evolves.
