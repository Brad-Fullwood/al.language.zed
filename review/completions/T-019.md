# T-019: Replace `PAGE_CONTROL_KEYWORDS` with LanguageData lookup
## Status: COMPLETED
## Changes
- File: `crates/al-syntax/src/symbols.rs`
- What: Removed the hardcoded `PAGE_CONTROL_KEYWORDS` const and `control_keyword_to_symbol_kind` match arm; replaced with `language_data::page_controls()` lookup driven by the JSON data file.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
Hardcoded keyword lists become stale with each Business Central release; driving lookups from `LanguageData` keeps them current without code changes and satisfies the project's no-hardcoded-AL-values rule.
