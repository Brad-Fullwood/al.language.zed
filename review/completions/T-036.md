# T-036: Replace `AlParser::parse_quick` with cached parse trees in query modules

## Status: PARTIAL

## Changes
- `crates/al-core/src/queries/arch_lint.rs`: switched to `get_cached_parse` — DONE
- `crates/al-core/src/queries/duplicates.rs`: switched to `get_cached_parse` — DONE
- `crates/al-core/src/queries/obsolescence.rs`: switched to `get_cached_parse` — DONE
- `crates/al-core/src/queries/sql_patterns.rs`: still uses `AlParser::parse_quick` — NOT DONE
- `crates/al-core/src/queries/tests.rs`: still uses `AlParser::parse_quick` — NOT DONE
- `crates/al-core/src/queries/test_coverage.rs`: still uses `AlParser::parse_quick` — NOT DONE
- `crates/al-core/src/queries/audit.rs`: still uses `AlParser::parse_quick` — NOT DONE
- `crates/al-core/src/queries/profiler_hints.rs`: uses `parse_quick` in production path (lines outside test cfg) — NOT DONE

## Validation
- Compiles: YES
- Tests pass for the three converted modules

## Justification
Three of eight modules were converted; five still re-parse on every query invocation. The remaining modules need the same treatment before T-036 is fully resolved.
