# T-016: Convert remaining recursive traversals (al-cli, al-test-harness, zed-al)
## Status: COMPLETED
## Changes
- File: `crates/al-cli/src/commands/mod.rs`, `crates/al-test-harness/src/protocol.rs`, `src/lib.rs`
- What: Converted `collect_al_files_recursive` (with added symlink cycle detection), `symbol_names`, and `merge_json` from recursive to iterative implementations.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
`collect_al_files_recursive` was vulnerable to symlink cycles causing infinite loops; `merge_json` runs in the WASM sandbox where stack size is constrained. Both risks are eliminated by iterative implementations.
