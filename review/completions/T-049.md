# T-049: Fix al-test-harness test assertions and flaky patterns
## Status: COMPLETED
## Changes
- File: `crates/al-test-harness/tests/e2e.rs`, `tests/edit_lifecycle.rs`, `tests/regression.rs`, `tests/completeness.rs`
- What: Removed redundant 10s poll loop in `test_diagnostics_published_on_open`; fixed vacuous assertion in `test_edit_b01`; added non-empty assertion to inlay-hint regression test; fixed `test_workspace_symbol_search` and the diagnostic-code test that never executed.
## Validation
- Compiles: YES
- Tests: PASS (943 tests, 1 pre-existing failure)
## Justification
Vacuous and no-op assertions give false confidence that features work; the fixes ensure the test suite actually verifies correct server behaviour rather than merely that functions return without panicking.
