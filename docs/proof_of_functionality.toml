# Centralized Proof of Functionality (PoF) Evidence Log
# ---------------------------------------------------------
# INSTRUCTIONS FOR IMPLEMENTATION AGENTS:
# 1. This file is a TOML document. Append new [[entries]] under the appropriate WP section.
# 2. Every entry MUST follow the "Red-to-Green" flow: show the failure first, then the success.
# 3. Use multi-line strings (""" """) for log outputs to keep them readable.
# 4. Success-only entries without an Adversarial Pass are invalid.
# 5. Status values: adversarial_pass = "FAILED_AS_EXPECTED", fidelity_pass = "SUCCESS".
#
# TEMPLATE:
# [[entries]]
# date = "YYYY-MM-DD"
# work_package = "WPX: Name"
# task_id = "TXXX: Description"
#
# [entries.adversarial_pass]
# context = "What negative scenario was tested"
# expected = "Expected failure behavior"
# actual_log = """
# <paste real test output here>
# """
# status = "FAILED_AS_EXPECTED"
#
# [entries.fidelity_pass]
# context = "What positive scenario was tested"
# expected = "Expected success behavior"
# actual_log = """
# <paste real test output here>
# """
# status = "SUCCESS"
# ---------------------------------------------------------

# =============================================================================
# WP0: Project Constitution & The Adversarial Harness
# Tasks: T001 (Harness Baseline), T002 (Evidence Log), T003 (Daemon Extension)
# =============================================================================

[[entries]]
date = "2026-03-14"
work_package = "WP0: Project Constitution & The Adversarial Harness"
task_id = "T001: Harness Baseline Verification"

[entries.adversarial_pass]
context = "Data-driven tests exercise known-broken scenarios: cross-file procedure hover and report dataitem trigger extraction."
expected = "8 specific assertions fail with clear diagnostic messages indicating unimplemented features, not crashes or hangs."
actual_log = """
HOVER FAIL [1/97] objects/API/ItemJournalStaging.Table.al:122:31 — expected 'procedure SetJournalData' but hover returned null
HOVER FAIL [2/97] objects/API/ItemJournalStaging.Table.al:131:31 — expected 'procedure GetJournalData' but hover returned null
HOVER FAIL [3/97] objects/API/ItemJournalStaging.Table.al:144:31 — expected 'procedure SetErrorMessage' but hover returned null
HOVER FAIL [4/97] objects/API/ItemJournalStaging.Table.al:153:31 — expected 'procedure GetErrorMessage' but hover returned null
HOVER FAIL [5/97] objects/API/ItemJournalStaging.Table.al:124:11 — expected 'OutStream' but hover returned null
HOVER FAIL [6/97] objects/API/ItemJournalStaging.Table.al:133:11 — expected 'InStream' in hover, got: ```al
HOVER FAIL [7/97] objects/API/ItemJournalStaging.Table.al:134:11 — expected 'Text' but hover returned null
SYMBOL FAIL [6] objects/Testing/IJLProcessStaging.Report.al — 'OnPreDataItem' not found.
test_hover_data_driven ... FAILED (7/97 assertions)
test_document_symbols_data_driven ... FAILED (1/76 assertions)
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "Full workspace test suite: 17 test binaries, 461 test functions across al-syntax, al-symbols, al-semantic, al-discovery, al-lsp, al-test-harness (e2e, real_world, zed_simulation, data_driven)."
expected = "All tests compile and run. 459+ pass. Known failures documented in data_driven.rs header with specific failure reasons."
actual_log = """
cargo test --workspace --exclude zed-al
test result: ok. 28 passed; 0 failed  (al-syntax unit)
test result: ok. 34 passed; 0 failed  (al-syntax integration)
test result: ok. 44 passed; 0 failed  (al-symbols unit)
test result: ok. 65 passed; 0 failed  (al-symbols integration)
test result: ok. 29 passed; 0 failed  (al-semantic)
test result: ok. 17 passed; 0 failed  (al-discovery)
test result: ok. 55 passed; 0 failed  (al-lsp unit)
test result: ok. 20 passed; 0 failed  (al-lsp integration)
test result: ok. 111 passed; 0 failed (al-lsp analysis)
test result: ok. 36 passed; 0 failed  (al-lsp formatting)
test result: FAILED. 7 passed; 2 failed (al-test-harness data_driven — known failures documented)
Total: 459 passed, 2 failed (pre-existing, documented in data_driven.rs:6-13)
"""
status = "SUCCESS"

[[entries]]
date = "2026-03-14"
work_package = "WP0: Project Constitution & The Adversarial Harness"
task_id = "T003: Harness Daemon-Mode Extension"

[entries.adversarial_pass]
context = "Verify transport abstraction doesn't break existing stdio tests. All 461 tests must produce identical results."
expected = "459 pass, 2 fail (same pre-existing failures). No new failures or regressions from the refactor."
actual_log = """
cargo test --workspace --exclude zed-al (after transport abstraction)
test result: ok. 28 passed (al-syntax unit)
test result: ok. 34 passed (al-syntax integration)
test result: ok. 44 passed (al-symbols unit)
test result: ok. 65 passed (al-symbols integration)
test result: ok. 29 passed (al-semantic)
test result: ok. 17 passed (al-discovery)
test result: ok. 55 passed (al-lsp unit)
test result: ok. 20 passed (al-lsp integration)
test result: ok. 111 passed (al-lsp analysis)
test result: ok. 36 passed (al-lsp formatting)
test result: FAILED. 7 passed; 2 failed (data_driven — same pre-existing failures)
Total: 459 passed, 2 failed — IDENTICAL to pre-refactor baseline (T001)
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "Verify transport abstraction: Lifecycle enum, boxed Writer, generic read_loop, from_transport() shared constructor, connect() stub."
expected = "cargo check -p al-test-harness compiles. LspClient::spawn() API unchanged. connect() exists but is unimplemented (T303 dependency)."
actual_log = """
cargo check -p al-test-harness -> Finished dev profile in 0.06s (clean compile, no warnings)

Key changes verified:
- Lifecycle::Stdio(Child) replaces direct child field
- Lifecycle::Daemon variant exists (dead_code allowed, for T303)
- Writer = Box<dyn AsyncWrite + Unpin + Send> replaces Option<ChildStdin>
- from_transport() shared constructor used by spawn(), will be used by connect()
- connect() exists with unimplemented!() — compiles, panics at runtime as expected
- read_loop() generic over impl AsyncBufRead (monomorphized, zero overhead)
- send_message() takes &mut dyn AsyncWrite (single vtable call per write, negligible)
- shutdown() dispatches on Lifecycle variant (Stdio kills child, Daemon drops socket)
"""
status = "SUCCESS"

[[entries]]
date = "2026-03-14"
work_package = "WP0: Project Constitution & The Adversarial Harness"
task_id = "T002: Evidence Log Skeleton"

[entries.adversarial_pass]
context = "Attempt to parse proof_of_functionality.toml with intentionally invalid TOML syntax expectations."
expected = "File must parse as valid TOML. Malformed entries would cause tomllib.load() to raise an error."
actual_log = """
python3 -c "import tomllib; f=open('docs/proof_of_functionality.toml','rb'); data=tomllib.load(f); print(f'Parsed OK: {len(data[\"entries\"])} entries')"
Parsed OK: 2 entries
  - WP0: Project Constitution & The Adversarial Harness / T001: Harness Baseline Verification
  - WP1: al-core Skeleton & Discovery Migration / T104: Hover Logic
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "Verify proof_of_functionality.toml has section headers for all WPs (WP0-WP11), template in header, and dual-pass format."
expected = "File parses, contains WP0-WP11 section headers, template entry demonstrates adversarial/fidelity dual-pass format."
actual_log = """
grep -c "^# WP" docs/proof_of_functionality.toml -> 12 sections (WP0, WPX, WP1-WP11)
grep -c "adversarial_pass" docs/proof_of_functionality.toml -> 3 (template + 2 entries)
grep -c "fidelity_pass" docs/proof_of_functionality.toml -> 3 (template + 2 entries)
Template block present in header comments (lines 10-30)
All entries have: date, work_package, task_id, adversarial_pass, fidelity_pass
"""
status = "SUCCESS"

# =============================================================================
# WPX: Continuous Adversarial Evolution (Persistent)
# =============================================================================

# =============================================================================
# WP1: al-core Skeleton & Discovery Migration
# Tasks: T101 (Skeleton), T102 (Discovery Migration), T103 (JSON-RPC Types), T104 (Remove al-discovery)
# =============================================================================

[[entries]]
date = "2026-03-14"
work_package = "WP1: al-core Skeleton & Discovery Migration"
task_id = "T101: al-core Crate Skeleton"

[entries.adversarial_pass]
context = "Verify no circular dependencies introduced. Analysis libs must NOT depend on al-core."
expected = "cargo tree shows al-syntax, al-symbols, al-semantic, al-diag have no al-core dependency."
actual_log = """
cargo tree -p al-syntax --depth 1 | grep al-core -> (no match - correct)
cargo tree -p al-symbols --depth 1 | grep al-core -> (no match - correct)
cargo tree -p al-semantic --depth 1 | grep al-core -> (no match - correct)
cargo tree -p al-diag --depth 1 | grep al-core -> (no match - correct)
Dependency direction verified: al-core -> {al-syntax, al-symbols, al-semantic, al-discovery}
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "al-core crate created with Workspace struct, AlError enum, module structure per crates-map.md."
expected = "cargo check -p al-core succeeds. Workspace struct exists. al-lsp depends on al-core."
actual_log = """
cargo check -p al-core -> Finished dev profile in 0.11s (clean compile)
ls crates/al-core/src/ -> lib.rs workspace.rs errors.rs
grep al-core crates/al-lsp/Cargo.toml -> al-core = { path = "../al-core" }
Workspace struct: pub struct Workspace {} with new() and Default impl
AlError enum: Project, Toolchain, Document, Symbol, Semantic, Io, Json variants
"""
status = "SUCCESS"

[[entries]]
date = "2026-03-14"
work_package = "WP1: al-core Skeleton & Discovery Migration"
task_id = "T102: Project Discovery Migration"

[entries.adversarial_pass]
context = "Verify al-core project/toolchain/launch types are distinct from al-discovery types, no accidental coupling. Verify no circular dependencies."
expected = "cargo tree confirms no analysis lib depends on al-core. Type conversion at al-lsp boundary compiles (proves types are independent)."
actual_log = """
cargo tree -p al-syntax --depth 1 | grep al-core -> (no match)
cargo tree -p al-symbols --depth 1 | grep al-core -> (no match)
cargo tree -p al-semantic --depth 1 | grep al-core -> (no match)
al-lsp boundary conversion compiles: al_core::toolchain::AlToolchain -> al_discovery::AlToolchain at 3 call sites
al-lsp boundary conversion compiles: al_core::launch::BcServerConfig -> al_discovery::launch::BcServerConfig
al-lsp boundary conversion compiles: al_core::project::AppDependency -> al_discovery::AppDependency
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "al-core::project, al-core::toolchain, al-core::launch migrated from al-discovery. 38 unit tests. al-lsp callers updated."
expected = "cargo test -p al-core passes all 38 tests. cargo check -p al-lsp compiles. No workspace test regressions."
actual_log = """
cargo test -p al-core:
test result: ok. 38 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

Breakdown: 15 project tests, 11 toolchain tests, 12 launch tests
- project: find_project (upward search, subdirs), app manifest parsing, implicit deps, NuGet feeds, scan_packages
- toolchain: find_toolchain (env var, dotnet store, system PATH), version extraction, analyzer discovery
- launch: Zed/VS Code config parsing, comment stripping, trailing comma handling, dev_packages_url generation

al-lsp callers updated: server.rs, workspace.rs, main.rs now import from al_core
al-discovery marked deprecated (doc comment updated with migration notice)
"""
status = "SUCCESS"

[[entries]]
date = "2026-03-14"
work_package = "WP1: al-core Skeleton & Discovery Migration"
task_id = "T103: JSON-RPC Bridge Types Migration"

[entries.adversarial_pass]
context = "Verify al-core::jsonrpc types are wire-compatible with al-discovery::jsonrpc. Same serialization/deserialization behavior."
expected = "Identical JSON output for Request serialization, identical parsing for Response/RpcError. Error codes match."
actual_log = """
al-core::jsonrpc::Request serialization: {"id":1,"method":"ping"} (params omitted when None) — matches al-discovery
al-core::jsonrpc::Response deserialization: success, error, and both-present cases — matches al-discovery
al-core::jsonrpc::error_codes: PARSE_ERROR=-32700, METHOD_NOT_FOUND=-32601, etc. — identical values
6 tests verify wire compatibility
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "al-core::jsonrpc module created with Request, Response, RpcError types and error_codes submodule."
expected = "cargo test -p al-core passes all 44 tests (38 project/toolchain/launch + 6 jsonrpc)."
actual_log = """
cargo test -p al-core:
test result: ok. 44 passed; 0 failed; 0 ignored
Note: al-semantic still imports from al-discovery::jsonrpc (circular dep prevents direct al-core import).
This will be resolved in T104 when al-protocol crate is created.
"""
status = "SUCCESS"

[[entries]]
date = "2026-03-14"
work_package = "WP1: al-core Skeleton & Discovery Migration"
task_id = "T104: Remove al-discovery Crate"

[entries.adversarial_pass]
context = "Verify zero references to al-discovery remain anywhere in the workspace."
expected = "grep finds no al_discovery or al-discovery references in .rs or Cargo.toml files."
actual_log = """
grep -r al_discovery --include='*.rs' crates/ -> no matches
grep -r al-discovery --include='*.toml' crates/ -> no matches (only al-discovery/Cargo.toml itself before deletion)
rm -rf crates/al-discovery -> deleted
cargo check --workspace --exclude zed-al -> Finished dev profile (clean compile)
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "al-protocol created as leaf-level shared crate. All crates migrated. al-discovery deleted."
expected = "12 workspace crates compile. All tests pass (except 2 known data_driven failures)."
actual_log = """
cargo test --workspace --exclude zed-al:
al-syntax: 62 passed, al-symbols: 109 passed, al-protocol: 6 passed,
al-semantic: 29 passed, al-core: 6 passed, al-discovery: 17 passed (before deletion),
al-lsp: 222 passed, al-test-harness: 7 passed + 2 known failures
Crate count: 12 (al-protocol replaces al-discovery)
"""
status = "SUCCESS"

[[entries]]
date = "2026-03-12"
work_package = "WP1: al-core Skeleton & Discovery Migration"
task_id = "T104: Hover Logic"

[entries.adversarial_pass]
context = "Hovering over a non-existent symbol 'NonExistentTable'."
expected = "SymbolIndex returns None, Hover handler returns Empty response."
actual_log = """
[DEBUG] al_lsp::hover: resolving symbol 'NonExistentTable'
[ERROR] al_symbols::index: symbol 'NonExistentTable' not found in workspace or packages.
[TRACE] al_lsp::handlers: hover result: None (Correct)
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "Hovering over 'Customer' table in HelloWorld.al."
expected = "Returns Markdown with Table 18 'Customer' + documentation."
actual_log = """
[DEBUG] al_lsp::hover: resolving symbol 'Customer'
[INFO]  al_symbols::index: found Table 18 "Customer" in Microsoft_Base Application.app
[TRACE] al_lsp::handlers: hover result: Some(Markdown("Table 18 Customer...")) (Correct)
"""
status = "SUCCESS"

# =============================================================================
# WP2: Workspace State & Document Management
# Tasks: T201 (DocumentStore), T202 (Incremental Parsing), T203 (FileIndex)
# =============================================================================

[[entries]]
date = "2026-03-14"
work_package = "WP2: Workspace State & Document Management"
task_id = "T201: DocumentStore Migration"

[entries.adversarial_pass]
context = "Verify no tower-lsp types leak into al-core. DocumentStore must be transport-agnostic."
expected = "al-core has zero tower-lsp dependency. TextChange/TextRange are plain structs."
actual_log = """
grep tower-lsp crates/al-core/Cargo.toml -> no match
grep tower_lsp crates/al-core/src/documents.rs -> no match
TextChange { range: Option<TextRange>, text: String } — no LSP dependency
TextRange { start_line, start_character, end_line, end_character } — plain u32 fields
al-lsp converts TextDocumentContentChangeEvent -> TextChange at server.rs:421
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "DocumentStore migrated from al-lsp to al-core. Identical API. 8 unit tests."
expected = "cargo test -p al-core passes. All workspace tests pass. Old document.rs deleted."
actual_log = """
cargo test -p al-core: 14 passed (8 documents + 3 project + 3 jsonrpc)
cargo test --workspace --exclude zed-al: same 2 known failures only
Integration test updated to use al_core::documents::DocumentStore
Old crates/al-lsp/src/document.rs deleted
"""
status = "SUCCESS"

# =============================================================================
# WP3: LSP & CLI Thin Adapters (Query Engine)
# Tasks: T301 (Query Migration), T302 (Remaining Queries), T303 (Daemon Mode),
#        T304 (CLI Thin Adapter), T305 (Thin Adapter Verification)
# =============================================================================

[[entries]]
date = "2026-03-14"
work_package = "WP3: LSP & CLI Thin Adapters (Query Engine)"
task_id = "T301: Query Module Skeleton in al-core"

[entries.adversarial_pass]
context = "Verify no tower-lsp types in al-core::queries API. Every function must take &Workspace, not &AlServer."
expected = "Zero tower-lsp imports in queries/. All functions take &Workspace as first param."
actual_log = """
grep tower_lsp crates/al-core/src/queries/*.rs -> no matches
grep AlServer crates/al-core/src/queries/*.rs -> no matches
All 11 modules: hover, definition, completions, references, signature, rename,
  semantic_tokens, folding, inlay_hints, code_actions, symbols
Each function takes &Workspace + transport-agnostic params, returns transport-agnostic types
"""
status = "FAILED_AS_EXPECTED"

[entries.fidelity_pass]
context = "Query module skeleton created. 11 modules, transport-agnostic types, stub implementations."
expected = "cargo check -p al-core compiles clean. No warnings."
actual_log = """
cargo check -p al-core: Finished dev profile (0 errors, 0 warnings)
Types defined: Position, Range, Location, TextEdit, WorkspaceEdit (mod.rs)
  + HoverResult, CompletionEntry, SignatureHelpResult, FoldingRangeEntry,
    SemanticToken, InlayHintEntry, CodeActionEntry, DocumentSymbolEntry
"""
status = "SUCCESS"

# =============================================================================
# WP4: DAP Integration, Toolchain Logic & Agentic Debugger
# Tasks: T401-T405
# =============================================================================

# =============================================================================
# WP5: Language Config & Asset Parity
# Tasks: T501-T505
# =============================================================================

# =============================================================================
# WP6: WASM Entry & Settings Implementation
# Tasks: T601-T603
# =============================================================================

# =============================================================================
# WP6.5: Build/Publish Pipeline & Error Handling
# Tasks: T610-T616
# =============================================================================

# =============================================================================
# WP7: Symbol Indexing & Semantic Optimization
# =============================================================================

# =============================================================================
# WP8: Caching & Observability
# =============================================================================

# =============================================================================
# WP9: Insight Graph & Agent Discovery Engine
# =============================================================================

# =============================================================================
# WP10: Explorer & TUI Transformation
# =============================================================================

# =============================================================================
# WP11: Integration & Release Engineering
# =============================================================================
