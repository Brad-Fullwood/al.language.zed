{
  "description": "AL Language debug adapter configuration for Business Central. Schema extracted from Microsoft AL Language extension v18.",
  "$comment": "Only fields consumed by the native adapter are exposed. Microsoft-only launch fields remain available by opting into al.useOfficialDap and using the Microsoft adapter's own configuration surface.",
  "type": "object",
  "required": [
    "adapter",
    "label",
    "request"
  ],
  "properties": {
    "adapter": {
      "type": "string",
      "const": "al",
      "description": "Debug adapter type"
    },
    "label": {
      "type": "string",
      "description": "Display name for this debug configuration"
    },
    "request": {
      "type": "string",
      "enum": [
        "launch",
        "attach"
      ],
      "description": "Debug request type. Snapshot initialization is not supported by this adapter; use `al-explorer snapshot start` instead."
    },
    "build": {
      "description": "Build step to run before debugging",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "command": {
              "type": "string"
            },
            "args": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "command"
          ]
        }
      ]
    },
    "authentication": {
      "enum": [
        "MicrosoftEntraID",
        "AAD"
      ],
      "default": "MicrosoftEntraID",
      "description": "Native DAP uses OAuth bearer authentication. Use al.useOfficialDap=true when Windows or UserPassword authentication is required."
    },
    "breakOnError": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "False",
            "True",
            "None",
            "All",
            "ExcludeTry"
          ]
        },
        {
          "type": "boolean"
        }
      ],
      "default": "All",
      "description": "Specifies whether the debugger to break on the next error. Accepts the string mode (`All`/`ExcludeTry`/`None`/etc.) or a boolean (`true` ≈ `All`, `false` ≈ `None`)."
    },
    "breakOnNext": {
      "enum": [
        "WebServiceClient",
        "WebClient",
        "Background",
        "ClientService",
        "Agent"
      ],
      "default": "WebServiceClient",
      "description": "Specifies that attach should break on the next available client session."
    },
    "breakOnRecordWrite": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "False",
            "True",
            "None",
            "All",
            "ExcludeTemporary"
          ]
        },
        {
          "type": "boolean"
        }
      ],
      "default": "None",
      "description": "Specifies whether the debugger breaks on record changes. Accepts the string mode (`All`/`ExcludeTemporary`/`None`/etc.) or a boolean (`true` ≈ `All`, `false` ≈ `None`)."
    },
    "dependencyPublishingOption": {
      "enum": [
        "Default",
        "Ignore",
        "Strict"
      ],
      "default": "Default",
      "description": "Specifies the publishing option to use. By specifying the Ignore option, only the current project is published, dependencies are ignored. By specifying the Strict option, publishing will fail if the project has apps that not part of the workspace and depend on it"
    },
    "enableLongRunningSqlStatements": {
      "type": "boolean",
      "default": true,
      "description": "Specifies whether the debugger enables or disables long running SQL statements in the debugger window."
    },
    "enableSqlInformationDebugger": {
      "type": "boolean",
      "default": true,
      "description": "Specifies whether the debugger shows the SQL Information in the debugger."
    },
    "environmentName": {
      "type": "string",
      "description": "Specifies which production, or sandbox environment to use."
    },
    "environmentType": {
      "enum": [
        "OnPrem",
        "Sandbox",
        "Production"
      ],
      "description": "Specifies which environment to use to connect to Business Central."
    },
    "launchBrowser": {
      "type": "boolean",
      "default": true,
      "description": "If the value is set to true, the browser tab is launched, if false, the browser tab is not launched. If publishing with debugging enabled, the browser tab is launched and this setting is ignored. The default value is true."
    },
    "longRunningSqlStatementsThreshold": {
      "type": "integer",
      "minimum": 0,
      "default": 500,
      "description": "Sets the number of milliseconds spent before a SQL statement is considered as long running in the debugger."
    },
    "numberOfSqlStatements": {
      "type": "integer",
      "minimum": 0,
      "default": 10,
      "description": "Sets the number of SQL statements to be shown in the debugger."
    },
    "port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "default": 7049,
      "description": "The development service port."
    },
    "schemaUpdateMode": {
      "enum": [
        "Synchronize",
        "Recreate",
        "ForceSync"
      ],
      "default": "Synchronize",
      "description": "Specifies the update mode to use when the extension is already installed."
    },
    "server": {
      "type": "string",
      "default": "http://bcserver",
      "description": "The server url."
    },
    "serverInstance": {
      "type": "string",
      "default": "",
      "description": "The server instance to connect to."
    },
    "sessionId": {
      "type": "integer",
      "minimum": -1,
      "default": -1,
      "description": "For attach requests, the id of the BC client session to attach to. Use -1 (the default) to attach via `breakOnNext` instead of a specific session."
    },
    "startupCompany": {
      "type": "string",
      "description": "Specifies the name of the company which should be opened after publishing."
    },
    "startupObjectId": {
      "type": "integer",
      "minimum": 0,
      "default": 22,
      "description": "Specifies the ID of the object which should be opened after publishing."
    },
    "startupObjectType": {
      "enum": [
        "Page",
        "Table",
        "Report",
        "Query"
      ],
      "default": "Page",
      "description": "Specifies the type of the object which should be opened after publishing."
    },
    "tenant": {
      "type": "string",
      "default": "default",
      "description": "The tenant to connect to.\nFor an on-premise server, this parameter must contain a tenant name, for example: MyTenant.\nFor a cloud server, this parameter must contain the Microsoft Entra tenant ID or Primary Domain associated with the Microsoft Entra tenant, for example: bf4ab6d6-cd93-424e-91fa-afe03fa286ac or mycustomer.onmicrosoft.com"
    },
    "validateServerCertificate": {
      "type": "boolean",
      "default": true,
      "description": "If the value is set to false, the server certificate will not be validated."
    }
  }
}
