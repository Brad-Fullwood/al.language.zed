[package]
name = "al-zed-test"
version = "0.2.0"
edition = "2021"
license = "MIT"
publish = false
description = "Linux/Wayland/Hyprland automation for end-to-end testing of the Zed AL extension"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }

# No dependencies on any other workspace crate — this is standalone tooling.
