# Usage

al-cli is the second frontend to the project — a CLI equivalent of what Zed gets via the LSP. Its primary consumers are AI agents (Claude) needing efficient codebase queries, and developers wanting CI/scripting tools.
