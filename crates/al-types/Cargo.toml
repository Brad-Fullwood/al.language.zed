[package]
name = "al-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared, dependency-free data types for the AL (Business Central) toolchain"
repository = "https://github.com/Brad-Fullwood/al.language.zed"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
