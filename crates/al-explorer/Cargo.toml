[package]
name = "al-explorer"
version = "0.2.2"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
al-protocol = { path = "../al-protocol" }
# Native .app emission (pure Rust, no Microsoft alc) for `pack-native`.
al-emit = { path = "../al-emit" }
crossterm = "0.29.0"
ratatui = "0.30.0"
serde = { workspace = true }
serde_json = { workspace = true }
# Folded in from al-cli (stage 8):
clap = { workspace = true }
clap_complete = { workspace = true }
dirs = "6"
url = "2"
