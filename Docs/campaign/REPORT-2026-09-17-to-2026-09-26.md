# Change Report: 2026-09-17 to 2026-09-26

## Executive summary

- The repository contains 583 commits in this reporting window.
- Recorded work starts on 2026-09-21; there are no commits from 2026-09-17 through 2026-09-20.
- The campaign branch now includes the formatter, ghost-diagnostics, and module-splitting workstreams.
- The focused validation completed successfully: formatting check, formatter property tests, workspace build, and the ghost-diagnostics regression test.
- The final campaign merge also incorporates the latest remote work through 2026-09-26, including runtime execution, test-router, XLIFF and additional module-splitting changes.

## Change volume

| Kind | Commits | Main areas |
| --- | ---: | --- |
| Documentation | 196 | Campaign state, findings, architecture, reference and plugin docs |
| Fixes | 230 | Trust, daemon security, analysis, syntax, runtime, LSP, extension and release behavior |
| Merges | 34 | Review rounds, queued fixes and completed workstreams |
| Refactors | 38 | Large module splits and smaller ownership/API cleanups |
| Features | 35 | Daemon, plugin, explorer, allocator, protocol and runtime capabilities |
| Tests | 23 | Harness, daemon, plugin, syntax, DAP and test-router coverage |
| Performance | 10 | LSP, symbol, syntax and analysis hot paths |
| Other | 14 | Build, CI, style, chore and benchmark maintenance |

## What changed

### Security and trust

- Added project-trust gates around privileged daemon operations and cached Business Central credentials.
- Added endpoint, peer, handshake-secret and credential-target checks for daemon connections.
- Contained caller-supplied paths, symlink traversal, UNC paths, executable selection and control-add-in resources.
- Removed repository text from security advisories and made the cleartext-HTTP escape explicit.
- Hardened plugin shell argument handling and release provenance/checksum reporting.

### Language server and daemon

- Added daemon capability declarations, safer dispatch routing, idle shutdown and version-aware replacement.
- Improved LSP re-indexing, diagnostics publication, concurrent dispatch, virtual-file handling and project scoping.
- Added resident-memory reporting, publish/free-ID operations, snapshot/profiling authorization and clearer protocol errors.
- Preserved the caller's text for read-only daemon methods when the daemon cannot open the path.

### Analysis, syntax and runtime

- Fixed object/member resolution, completions, tooltips, permissions, generated identifiers, outline quoting and source ranges.
- Fixed formatter ordering and idempotence, including malformed-file behavior and multi-object sorting.
- Fixed runtime defaults, numeric rounding, text/code capacity, record variables, call depth and dispatch behavior.
- Split the large analysis resolution and runtime dispatch implementations into focused module directories.
- Added local execution for enum variables and methods, chained calls, arrays, text indexing, built-ins, `CalcSums`, case labels and record operations.
- Split XLIFF, code lenses, source indexing, protocol clients and test-router code into focused modules with dedicated tests.

### Business Central, DAP and explorer

- Unified Business Central credential authorization and improved package/source discovery and navigation.
- Split native DAP and BC debug session implementations into module directories without intended behavior changes.
- Improved debug event routing, step reasons, SignalR bounds, source access and shutdown behavior.
- Added explorer commands and presentation tests for publish, free IDs, profiling, details and trust workflows.

### Plugin, CI and documentation

- Added and refined Claude Code plugin skills, subagents, session hooks, validation and marketplace metadata.
- Added CI/release gates, dependency updates, toolchain pinning and release-dry-run coverage.
- Updated campaign logs, findings, architecture diagrams, feature docs, settings, README guidance and blog planning.

## Branch resolution

- `campaign/2026-09-21` is the integrated branch.
- `campaign/fix-formatter-idempotence` was merged at `a2af58f5`.
- `campaign/fix-ghost-diagnostics` was merged at `fcb3e3f1`.
- `campaign/slop-splits-2` was merged at `3527a2e8`; its unfinished BC debug session split was committed as `354ec882` first.
- The three workstream branches remain available on the remote as historical, fully merged branches.
- No uncommitted changes remain in the main checkout or the active Claude worktrees.

## Validation

- `cargo fmt --all -- --check` — passed.
- `cargo test -p al-syntax --test property_sort_members` — 4 passed.
- `cargo build --workspace` — passed.
- `cargo test -p al-test-harness --test regression no_ghost_diagnostics_after_close_during_debounce` — 1 passed.

## Complete inventory

The complete per-commit inventory for this report is the Git history from `a8e54fb7` through `3527a2e8` on `campaign/2026-09-21`:

```text
git log --since='2026-09-17' --date=short --format='%h %ad %s' campaign/2026-09-21
```
