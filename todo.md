1. Combine cli into lsp, will this will minimise code duplication as the cli is basically an lsp wrapper anyway.
2. I think the crates are all their own binaries? If so why are they not libraries? with just the lsp being a binary.
